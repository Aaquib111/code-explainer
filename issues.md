- Dont care about TTS, just want macos say with playback speed support
- Don't care about quizzing the user, always want walkthroughs (with the other parameters inferred from the conversation with the user so far)
- wayyy too many subagents, want a minimal set of skills for funcationality
  - Codex paths aren’t recognized.
  - The documented macOS say fallback doesn’t exist.
  - Setup reports success after a failed model download.
  - TTS failures aren’t surfaced in the sidebar.
  - Authentication files live somewhere Codex cannot access.
  - The setup/install/copy flow creates duplicate installations.

